import styled from 'styled-components'

export const FooterContainer = styled.div`
  display: grid;
  width: 100%;
  background-color: ${({ theme }) => theme.colors.black};
  grid-area: footer;
  padding: ${({ theme }) => theme.spacings.normal};
`

export const GroupContainer = styled.ul`
  display: flex;
  flex-direction: column;
  gap: ${({ theme }) => theme.spacings.normal};
`
export const GroupTitle = styled
export const GroupLink = styled.li``
